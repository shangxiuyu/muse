# 前端验收记录

## 文案参考独立化（2026-09-22）

- 文案入口改为 8 类专用参考：清晰说明、温和同行、鲜明观点、故事叙述、轻快分享、产品推介、极简短句、专业汇报。每项带适用场景，预览展示原创例句与表达方式，不再显示 UI 配色。未选择时随内容判断。
- 桌面悬停和键盘聚焦只预览，点击或 Enter 选用；手机点选项预览、点“选用此文风”确认。验证了 End / Tab / Enter / Esc、切换媒介恢复、移除、保留输入和 44px 确认按钮。390px 与 320px 无横向溢出。
- 已观察[桌面浅色](../output/qa/text-reference-desktop.png)、[深色](../output/qa/text-reference-dark.png)、[手机](../output/qa/text-reference-mobile.png)与[窄屏](../output/qa/text-reference-narrow.png)截图，控制台无错误。手机例句按段落紧凑展示，超长内容可滚动，确认按钮保持可见。
- 新增写作规范与后端媒介校验；所选规范进入 Pi 任务上下文，随消息、作品和版本保存并可导出。旧文案视觉参考保留历史记录，重开与重试不恢复成新的文案选择；旧 PPT 视觉参考保持兼容。
- 全部 69 项测试通过，覆盖规范读取、保存与替换、移除、导入、跨媒介拒绝、失败与取消后重试、源文件导出及真实 Pi SDK 在本地模拟模型服务中的上下文传递。没有调用付费模型评估实际文案质量。仓库自审 0 error / 0 warning；UI 检查保留原有两个系统层级提醒。
- 本机服务已重新加载并确认新增参考可读取，原作品 ID 完整保留。

2026-09-22；使用 gstack browse 的真实 Chromium 检查。


## 参考标签替换底部入口（2026-09-22）

- 所选参考移到输入框底部，直接替换“添加参考”或“图片风格”。点击标签可更换，移除后原位恢复入口；不再单独占一行或重复显示入口。
- 26 项浏览器检查通过，覆盖 UI、PPT、文案、图片的选中、更换、移除、焦点恢复、输入内容及节点保留；工作台的作品预览节点也保持不变。
- 390px / 320px 下仅保留一个参考入口，无横向溢出，标签不挤压发送按钮。已观察[桌面](../output/qa/reference-toolbar-desktop.png)与[手机](../output/qa/reference-toolbar-mobile.png)效果；JavaScript 语法通过，浏览器无控制台错误。

## Muse skill 品味导入（2026-09-22）

- 品味页提供本机导入、文件选择与拖放入口，显示并支持复制 Muse 品味目录。只读取固定的两个偏好 YAML 文件，环境变量覆盖默认路径；预览时不写入工作空间。
- 隔离工作空间验证本机读取、取消勾选、调整适用作品、保存、刷新恢复、文件上传、格式错误后重选及重复导入。两条已选偏好正确保存；再次上传含一条重复记录的文件，只新增一条。导入保留未提交的手写偏好。
- 修复文件选择器的 cancel 事件冒泡导致关闭预览弹窗的问题。手机弹窗内部滚动，标题与保存按钮保持可见；390 × 844 无横向溢出。已观察桌面浅色、深色和手机截图，记录在 `.test-results/memory-import/`。
- 仓库自审 0 error / 0 warning，48 项测试通过。新增覆盖 confirmed 筛选、场景与例外保留、YAML / JSON 解析、缺失目录、越界链接、体积限制、原子写入、容量限制、重复导入与旧模型上下文清理。导入流程不调用模型，测试未改动真实品味文件。

## 文案右侧预览（2026-09-22）

- 已用实际保存的咖啡店对话验证：没有作品版本时仍在右侧显示正文，保留标题、列表与强调，排除开场介绍及设计判断；原始对话不变，刷新后恢复。
- 原文切换、返回预览正常。拦截浏览器剪贴板调用与下载 Blob，验证复制和 Markdown 导出的内容与正文完全一致，未覆盖系统剪贴板。
- 已观察 1440 × 1000 [浅色](../output/qa/text-preview-light.png)、[深色](../output/qa/text-preview-dark.png)及 390 × 844 [手机](../output/qa/text-preview-mobile.png)。手机预览可滚动，390px / 320px 布局稳定后均无横向溢出；浏览器无控制台错误。
- 仓库自审通过，42 项测试通过。真实 Pi SDK 经本机模拟接口验证遗漏提交时恢复文案、保存版本、修改父版本、重启恢复与闲聊不新增版本；本轮未调用付费模型。


## UI 风格悬停预览（2026-09-22）

- 网页 UI 的 10 个母体复用已有 1280 × 900 示例截图；鼠标悬停与键盘聚焦同步更新菜单旁的图片、风格说明和来源，选中状态只在确认选用时改变。作家案头标注为 Muse 页面示例，其他图片保留网站参考署名。
- 26 项浏览器检查通过：10 张图片正确加载、逐项预览不误选、键盘聚焦、点击选用与关闭、重新打开恢复选择、浏览其他风格保留原选择，以及输入内容和输入节点保留。另验证方向键、Home / End、Enter、点击外部关闭与 Esc 焦点恢复。
- 390 × 844 与 320 × 700 各通过 8 项窄屏检查。预览移到列表上方，点选项先看图，再点“选用此风格”确认；Tab 可到达确认按钮。列表可滚动，选项与确认按钮至少 44px，面板位于视口内，无横向溢出。另验证 320 × 360 的较矮窗口，面板利用视口空间保留可操作列表和确认按钮。
- 已观察桌面[浅色](../output/qa/ui-reference-hover-light.png)、[深色](../output/qa/ui-reference-hover-dark.png)与[手机](../output/qa/ui-reference-hover-mobile.png)截图。浏览器无控制台错误，JavaScript 语法与仓库自审通过；UI 检查保留原有跳转正文及系统提示的层级提醒。
- 本次只修改 UI 参考的前端预览与确认交互，未调用模型生成，也未修改已保存的作品内容。

## PPT 悬停样张预览（2026-09-22）

- 为 5 个 PPT 参考制作并截取 1280 × 720 的原创演示样张，[素材与源码](assets/presentation-styles/README.md)可追溯。打开菜单预载图片，悬停与键盘焦点只切换预览，点击或 Enter 才选用。
- 已验证五种图片切换、移入预览区保持展开、选中状态不被悬停改变、重新打开显示所选风格、方向键 / Home / End、Esc / Tab、点击外部关闭、焦点恢复与未发送输入保留。菜单仍贴近入口，右侧不足时可向左放置预览。
- 已观察 1440 × 1000 [浅色](../output/qa/ppt-hover-light.png)、[深色](../output/qa/ppt-hover-dark.png)与 390 × 844 [手机](../output/qa/ppt-hover-mobile.png)。390px / 320px 的堆叠菜单均在视口内，无页面横向溢出；可用高度很小时优先显示选项。
- 37 项测试通过，包含新增图片格式与尺寸校验。独立浏览器会话无控制台错误。UI 检查无错误，保留原有跳转正文与系统提示的层级提示。

## PPT 专属参考（2026-09-22）

- 复用 Muse 现有演示流派规范，PPT 提供商务咨询、燕麦刊物、科技发布、职场研报、积木波普 5 个参考。网页 UI 与文案保留原有 10 个母体，图片继续使用图片风格面板。
- 浏览器已验证选择、替换、选中标记、移除、各创作类型分别保留选择、输入文字与输入节点保留，以及新创作清空；方向键、Home / End、Enter、Esc / Tab、外部点击和焦点恢复正常。
- 已观察 1440 × 1000 [浅色](../output/qa/ppt-reference-desktop.png)、[深色](../output/qa/ppt-reference-dark.png)及 390 × 844 [手机截图](../output/qa/ppt-reference-mobile.png)。390px 与 320px 菜单均位于视口内，手机选项高度为 44px，无页面横向溢出，控制台无错误。
- 全部 37 项测试通过。新增检查覆盖规范章节、中文排版规则、生成请求、磁盘保存、替换与移除、失败与停止后的保留、备份导入、跨媒介拒绝与旧参考兼容。真实 Pi SDK 经本地模拟接口验证所选规范进入上下文、下一轮移除后为空；未消耗真实模型额度，未评估模型实际生成的幻灯片质量。

## 移除顶栏并集中设置入口（2026-09-22）

- 顶部工作空间栏已移除。1440 × 1000 桌面工作台从 y=0 开始，高度为 1000px；768 × 1024 下仍保留 80px 图标导航，无横向溢出。
- 左下角设置集中外观、模型连接、备份与“认识 Muse”。已验证深浅切换、关于与设置往返、Escape 关闭及焦点回到入口；未发送文字、输入节点和作品 iframe 均保留。
- 390 × 844 手机工作台仅保留 52px 的导航/视图切换行；320 × 700 下工作区恰好到达视口底部，均无横向溢出。手机抽屉、设置、关于返回、关闭后的 inert 与焦点恢复正常；320px 首页导航按钮不遮挡标题。
- 已观察桌面首页、全高工作台、深色设置、手机工作台和手机首页截图。JavaScript 语法与仓库自审通过，浏览器无控制台错误；UI 检查无底线错误，仅保留原有跳转正文和系统提示的层级提示。
- 本次只核对入口迁移及布局交互，未发起模型生成或修改已有作品内容。

## 作家案头参考图纠正（2026-09-22）

- 原 Lex 视频封面与作家案头母体的燕麦纸、琥珀聚焦特征不对应。已按规范制作独立的 [Muse 页面示例](examples/writer-atelier.html)，并用真实浏览器截图替换 `assets/archetypes/writer_atelier.png`。数据源记录改为 Muse 示例，不再归于 Lex。
- 已检查 1280 × 900 桌面效果与 390 × 844 手机布局，手机无横向溢出；专注模式与 Escape 退出正常，浏览器无控制台错误，修改的 JavaScript 语法检查通过。
- 修正时曾验证图片加载、正确来源、大图与选用。随后并行任务将入口改成紧凑列表，当前界面不再展示图片，故此前大图检查和 `output/qa/writer-atelier-*-fixed.png` 仅记录中间版本；最终保留的是纠正后的素材与独立示例页。

## 内置母体与真实网站截图（初版记录，后续已改为小列表）

- “添加参考”改为 Muse 内置的 10 个母体，移除本地文件选择入口。每个母体配有一张 1280 × 900 的真实官网截图；详情支持放大、访问来源、返回列表和选用。截图本地保存，合计约 2.1 MB，列表按需加载。
- 18 项浏览器交互检查通过：10 个选项及图片加载、放大与返回、选中状态、更换和移除、保留输入节点及 iframe、消息和版本保存、停止后恢复、新创作清空及重开作品恢复。选择不写入长期品味。
- 已观察 1440 × 1000 明暗选择面板与放大详情、390 × 844 手机界面。390px 和 320px 无页面横向溢出；手机单列，内容区可滚动，操作按钮保持可见。浏览器无控制台错误。
- 后端及母体测试共 12 项通过；10 个 ID 对应仓库全部母体规范，非法 ID 被拒绝，完整规范进入创作上下文，母体随请求、对话及版本保存。测试使用隔离工作空间和模拟生成引擎，未消耗真实模型额度。
- 最新仓库自审：0 error / 0 warning；JavaScript 语法检查通过。截图来源见[记录](assets/archetypes/README.md)。

## 连贯交互动效（2026-09-22）

- 新增独立 motion 模块与样式，无外部依赖；只动画 transform / opacity，页面切换编排和局部反馈分开，输入时不重播页面动画。
- 21 项桌面检查通过：页面和弹窗起始/中间/结束状态、选中底色移动及清理、输入后发送键反馈、行内方向面板、弹窗退出与焦点恢复、快速明暗切换、导航展开与中途反向、输入/iframe 保留、PPT 翻页、源内容进入、等待反馈结束清理等。
- 实际时间采样：标题 opacity 0 → 0.80 → 1；弹窗 opacity 0 → 0.82 → 1；导航主区 translateX -144 → -35.64 → 0。已查看媒介选择、导航运动的中间截图，以及手机弹窗开始、中间、结束三张截图。
- 6 项手机抽屉检查通过：滑动的中间状态、展开可操作、关闭不重建输入、关闭后 inert 与焦点回到导航按钮、快速反向、无横向溢出。
- 减少动态效果的 JS 分支以受控 MediaQueryList 验证：跳过位移，关闭弹窗和主题操作立即执行；CSS 提供对应媒体查询来禁用过渡、循环和延迟。未切换用户真实操作系统设置。
- CSS 按实际加载顺序合并进行 Muse 检查，无新增底线错误；保留原有系统层级及中文字号提示。JavaScript 语法检查通过。独立 motion.css 的跨文件 token 提示不是运行时缺失。
- 生成完成反馈已在本轮原有体验样稿流程验证。并行加入的真实后端请求不属于本次动效验收；当前静态服务的 API 404 与仓库新增 server/prompt.md 的孤立文档检查不由本次动效修改引起。

## 创作工作台默认折叠导航（2026-09-22）

- 对话与作品预览状态默认采用 80px 图标栏；首页仍使用完整导航。底部支持手动展开和折叠，每个图标按钮保留可访问名称与悬停说明。
- 10 项浏览器检查通过：默认折叠、手动展开/折叠、输入及 iframe 节点不变、首页完整导航、PPT 页码保留、重开作品折叠、新对话即时折叠、停止生成保留折叠状态、无横向溢出。
- 已观察 1440px 深色工作台截图；768px 仍为 80px 栏且不溢出。390px 手机使用 248px 完整抽屉，标签可见，桌面展开按钮隐藏，Escape 正常关闭。浏览器无控制台错误，JavaScript 语法检查通过。

## 输入区重复描边修正（2026-09-22）

- 去除 composer 内 textarea 的独立聚焦轮廓，统一由输入区外框呈现黄色聚焦反馈；其他表单及工具按钮保留自己的键盘焦点提示。
- 已观察深色真实截图，并验证浅色键盘 Tab 返回输入框时内部无描边、外框仍显示聚焦色。

## 用户选定第一版、暖黄主题与深色模式（2026-09-22）

- 删除两种替代布局、风格切换栏、对比总览及相关截图；旧 `?style=` 地址自动回到原版，不清除作品与品味。
- 从用户确认的图标取主要黄色 #F9C93C；全界面通过语义颜色变量支持浅色与暖炭灰深色，作品示例保持自身色板。
- 14 项浏览器交互检查通过：保留原布局、品牌色与主按钮、切换时输入节点不重建、未保存品味及媒介保留、跟随当前系统设置、弹窗内选中反馈、深色弹窗、四种示例、PPT 页码及 iframe 保留、作品数据不变、深色源内容及桌面溢出检查。
- 另已验证浅色和深色刷新后恢复、旧比较链接兼容、320/390/768/1440px 布局无横向溢出、移动切换按钮为 44px。桌面首页、PPT 工作台、设置弹窗及手机首页均已截图观察；窄屏外观选项隐藏辅助图标，保持文字完整。
- 文本语义色在界面表面色上的最低对比度：浅色 4.62:1、深色 5.50:1；黄色主按钮文字 9.46:1。该数值覆盖界面颜色 token 组合，不代表所有示例作品或完整辅助技术审核。
- 浏览器无控制台错误；JavaScript 语法与 Muse 仓库自审通过。UI 提示仅保留原有的跳转正文 / 系统提示高层级和已说明的中文光学字号。
- 系统模式监听 `prefers-color-scheme`，首屏前应用已保存外观；未操作真实操作系统外观开关进行端到端验证。

## 暖黄手绘图标接入（2026-09-22）

- 1440 × 1000 桌面与 390 × 844 手机：新品牌图片加载成功，页面无横向溢出；手机抽屉内品牌图标为 44px。
- 已观察首页、对话头像、作品空白状态与关于弹窗，均使用用户确认的暖黄色底图片。浏览器中的临时创作测试已还原。
- 页面图标为 256px PNG，约 65 KB；32/64px 浏览器图标和 180px 主屏幕图标均返回 HTTP 200。
- 本次修改后的 JavaScript 语法检查通过；项目自审 0 error / 0 warning。

## 已观察

- 1440 × 1000、1280 × 900 桌面：创作首页与左右分栏工作台。
- 390 × 844 手机：单列布局、抽屉导航、对话/作品切换、PPT 翻页；320px 宽度检查无页面横向溢出。
- 网页、PPT、图片实际渲染已截图观察；文案 DOM 的正文与标题已验证。
- 20 项交互检查通过：追加版本、源内容、预览隔离、筛选、PPT 上下页、文案与图片预览、品味保存与删除、作品搜索与重命名、删除确认及取消、中文输入法 Enter 不误发、加载反馈、停止后恢复输入、桌面无横向溢出。
- 刷新后作品历史仍在。四类导出均捕获实际 Blob 并核对类型和内容：HTML 网页、HTML Web Deck、Markdown、SVG。
- 最后一次检查浏览器无控制台错误；所有 JavaScript 语法检查通过。
- Muse UI 检查无底线错误。提醒中的高 z-index 仅用于跳转正文链接和系统提示；中文工具字号的光学例外已在设计契约说明。缩略图的小字属于示例作品本身，作品名称和类型在图外重复提供。

## 范围

本轮按用户要求只实现前端。对话使用清楚标记的原创体验样稿，没有连接模型；后续修改演示版本机制，不声称真实改写。PPT 导出为 HTML Web Deck，未实现 .pptx。未进行独立用户审美评估、完整辅助技术审核或跨浏览器兼容测试。


## 我的作品改版验收 · 2026-09-22

本次在现有真实后端上检查「我的作品」改版，以下结果仅对应本次改动：

- 桌面 1440×960 深色与浅色预览；390×844 手机卡片与列表、320px 窄屏检查无横向溢出。
- 真实作品预览、搜索保留焦点、分类与搜索组合、三种排序、空结果恢复、卡片/列表切换通过。重命名入口、删除确认取消、打开已有作品通过；未修改或删除用户作品。
- 按用户追加要求，仅展示具备具体产出的非样稿作品；真实工作空间原有 10 条记录展示为 4 件作品。作品总数、各媒介计数、侧栏与最近创作一致，闲聊和样稿不出现在搜索结果中。
- 使用独立数据验证闲聊、未生成、空产物、显式样稿及旧样稿被过滤；四类实际作品、样稿经真实修改后的作品、从完成对话恢复的文案正常保留。全部隐藏时使用首次创作空状态，原始数据不变。
- 新增代码语法检查、局部 UI 检查通过；现有 Markdown 和对话文案恢复相关 12 项测试通过；最终浏览器无控制台错误。
- 截图：`../output/collection-redesign/desktop-dark.png`、`mobile-dark.png`、`mobile-list.png`。缩略图不执行作品脚本；完整作品继续使用原有预览与导出能力。

- 工具栏微调：移除独立作品统计，排序与视图切换移到搜索左侧。1440px 实测同排且位置正确，320px 无横向溢出；搜索和排序通过，浏览器无控制台错误。

- 缩略预览修正：移除网页顶部浏览器装饰栏，统一 16:9 画幅。PPT 直接渲染第一页原始内容，不套额外 slide 容器或共享演示样式；实测只有 1 个页面，卡片与列表中均无多余页面背景，控制台无错误。
# 源文件与设计系统导出 · 2026-09-22

- 预览区「导出」增加源文件包选项，按打开弹窗时的当前版本打包；保留单文件导出。
- 62 项自动测试通过，覆盖四种媒介、源码保真、逐页 HTML 与讲者备注、实际样式提取、版本信息、参考选择与清除、PNG/JPEG/WebP 原始字节及 ZIP 路径和校验码。规范接口仅接受内置 ID，验证了路径穿越拒绝和跨站访问限制。
- browse 实际点击「一块咖啡 · 品牌介绍」的源文件下载，ZIP 为 99,904 字节；用独立 ZIP 读取器验证全部文件校验码并解压，包含 4 页 HTML、原始内容、讲者备注、10 个静态颜色值和「积木波普」设计规范，共 15 个文件。
- 模拟设计规范接口失败：弹窗保留并提示错误，未生成缺少规范的下载；恢复接口后重试成功。原有 HTML 单文件下载保留 4 页及翻页逻辑。
- 桌面浅色与 375px 深色弹窗检查通过，无横向溢出；Escape 关闭动画结束后焦点回到导出按钮，浏览器无控制台错误。版本元数据由自动测试覆盖，当前真实工作空间没有多版本作品可做切换验收。
- 设计清单为源码的静态整理，未推测不存在的语义变量；位图不包含虚构图层，外部资源引用原样保留。规范按版本关联 ID 从本机规范库读取。
- 截图：`../output/source-export-dialog.png`、`../output/source-export/mobile-dark.png`；实际导出样本：`../output/source-export/一块咖啡-源文件.zip`。

## 图片预览按原图比例显示（2026-09-22）

- 移除预览框固定的 8:9 比例，SVG 按 viewBox（无 viewBox 时读取绝对宽高）、位图按实际像素宽高确定预览比例。高度限制同时约束宽度，避免竖图或长图被单独压缩高度后出现白边；预览框背景透明。
- 1440 × 1000 桌面及 390 × 844、320 × 700 手机尺寸下，横图、方图、竖图、超宽图、长图、PNG 和仅声明宽高的 SVG 共 21 次尺寸检查通过。截图中的 1440 × 900 海报在桌面显示为 600 × 375，上下白边已消除。
- 已验证版本切换、源内容切换、桌面导航展开收起后比例正常；作品源码与图片沙箱保持一致，无页面横向溢出，浏览器无控制台错误。已查看桌面与手机修复截图：`output/qa/image-preview-landscape-fixed.png`、`output/qa/image-preview-mobile-fixed.png`。
- JavaScript 语法及仓库自审通过；UI 检查无底线错误，保留原有 2 项系统层级提示。检查使用独立临时服务及样例数据，未调用模型生成或改动用户作品。
