# IMAGE.md 图像任务契约与资产索引

<!-- muse:genre spec -->

本文件是示例，不是已经执行的任务记录。复制时替换示例内容，删去不适用字段；路径仅示意命名方式，不表示文件存在。操作规范见[图像工作流](references/toolkit/image_visual_tool.md)。

项目输出目录中的一份账本关联逐张提示词和结果，不将全部版本塞进一段提示词。一次性单图可将必要信息合并到提示词记录；不覆盖 skill 自带的本范例。

## 情境与约束

- **任务**：为一篇讲解“知识从收集到复用”的文章制作两张解释图。
- **受众与用途**：初次建立知识库的读者；正文阅读，不承担现场或产品效果证据。
- **内容来源**：示例设定；真实执行前核对文章原文，不编造效果数字。
- **主要主张**：保存资料不等于能够复用，检索与应用之间需要明确连接。
- **信息结构**：IMG-01 对比；IMG-02 流程。
- **表现语言**：用户指定的平面图解；不是所有任务的默认风格。
- **配色**：沿用本次品牌色，具体色值待提供；不从范例自动继承。
- **交付**：两张 16:9 PNG；文字为中文，必须准确。尺寸按版位与工具能力确认。
- **范围**：只生成配图，不修改文章，不对外发布；不增加额外候选。
- **共享约束**：相同线条和文字层级、颜色语义一致；构图按每张内容分别安排。

## 逐张任务

| ID | 模式／版本 | 位置与职责 | 必须保留／准确内容 | 提示词记录（拟） | 结果与状态 |
|---|---|---|---|---|---|
| IMG-01 | 新图／v01 | 定义段后；比较“仅收集”与“能复用” | 两侧标题准确，不暗示未经证实的效率数字 | `prompts/IMG-01-v01.md` | 尚未生成；planned |
| IMG-02 | 新图／v01 | 方法段后；解释三个步骤的关系 | “收集 → 检索 → 应用”的顺序 | `prompts/IMG-02-v01.md` | 尚未生成；planned |

## 参考与保留约束

| 参考 ID | 实际来源与访问情况 | 用途 | 迁移边界 |
|---|---|---|---|
| 无 | 本示例未提供参考图 | 不适用 | 不编造“已读取／已传入”的参考 |

编辑任务在对应条目补充：基准图和父版本、保留项、变化项、区域、比对方法。系列涉及同一人物或产品时，明确实际共享参考，不能只记录“风格一致”。

## 单张提示词与执行记录示例

下列内容拟保存到 IMG-02 的提示词记录。只有“最终提示词”正文交给模型，其他字段通过支持的参数或任务记录保留；实际请求必须包含对应的原图或参考输入，不能只写文字路径。

**最终提示词**

> 制作一张横向平面流程图，解释知识从收集到应用的过程。从左到右依次呈现“收集”“检索”“应用”三个区域，用清晰的连接线表明顺序。收集区呈现资料汇入，检索区呈现从资料中找出相关项，应用区呈现相关项组成一个回答。只显示上述三个中文标签，不添加数字、Logo 或装饰性文字。各区通过图形关系而非写实材质区分，颜色承担分组职责，连接线与文字在正文阅读尺寸下清晰可辨。

**执行信息**

- 图片／版本：IMG-02／v01；父版本：none。
- 风格选择：custom（本例由平面图解要求直接构思）；选择依据：解释步骤关系；保留特征：清楚轮廓与连接；本次调整：沿用任务品牌色。使用内置卡片时记录[风格 ID](references/image_styles.md)及实际调整，不必把整张卡复制进账本。
- 实际执行器／提供方／模型：unknown，尚未选择或调用。
- 请求参数：目标 16:9；具体字段名和像素尺寸待能力检查后记录，不把比例保证寄托在提示词文字上。
- 参考输入：无。
- 预期输出位置：`images/IMG-02-v01.png`；实际返回位置：无。
- 服务端任务 ID／尝试次数：无／0。
- 状态：planned。提示词与参数以后变更时保留版本关系。
- 可复现范围：能追踪请求意图，不保证重新生成与原图一致。

## 验收记录

| 图片／版本 | 已观察内容 | 结论 | 下一步 |
|---|---|---|---|
| IMG-01／v01 | 未生成，未观察 | 未验证 | 执行后核对对比关系、标题与正文版位 |
| IMG-02／v01 | 未生成，未观察 | 未验证 | 执行后核对标签、箭头顺序、真实尺寸和格式 |

实际执行后记录图片版本、观看尺寸、观察现象及保留项对照。生成成功记为 generated；看图和文件核验满足本次要求后才记 accepted。仍有缺陷记 needs_revision；技术失败记 failed；任务状态不明记 unknown。

## 失败、修订与交付记录

- 失败项及原因：暂无执行记录。
- 重试／修订：记录对应图片、父版本、修改目标和实际尝试，不重做已经成功且通过验收的条目。
- 最终选用版本：未选定。
- 实际交付文件与预览：无，不能报告已交付。
- 未验证范围：全部；本示例未调用模型，也未观察图片。
